# ⚙️ Конфигурация дашборда

Этот документ содержит инструкции по настройке дашборда под ваши нужды.

## 📝 Содержание

- [Базовая конфигурация](#базовая-конфигурация)
- [Изменение нормативов](#изменение-нормативов)
- [Изменение операций](#изменение-операций)
- [Изменение цветов](#изменение-цветов)
- [Изменение интервала обновления](#изменение-интервала-обновления)
- [Добавление новых графиков](#добавление-новых-графиков)
- [Изменение порта сервера](#изменение-порта-сервера)

---

## Базовая конфигурация

Откройте файл `dashboard.html` в текстовом редакторе и найдите секцию:

```javascript
const CONFIG = {
    EXCEL_FILE_ENDPOINT: '/excel',
    REFRESH_INTERVAL_MS: 60000,      // 60 секунд
    DATE_RANGE_DAYS: 30,             // 30 дней
    TOP_N: 3,                        // ТОП-3 сотрудника
    
    COLORS: {
        LOW: '#ff3b3b',              // Красный (0-70%)
        MID: '#ffcc00',              // Жёлтый (70-100%)
        HIGH: '#00b050'              // Зелёный (100%+)
    }
};
```

### Параметры:

- **EXCEL_FILE_ENDPOINT** - путь к API для загрузки Excel файла
- **REFRESH_INTERVAL_MS** - интервал автообновления в миллисекундах
- **DATE_RANGE_DAYS** - количество дней для фильтрации данных
- **TOP_N** - количество сотрудников в ТОП-листе
- **COLORS** - цвета для индикации достижения нормы

---

## Изменение нормативов

### Логика B (Старый дашборд)

Найдите секцию `LOGIC_B` и измените значения `norm`:

```javascript
const LOGIC_B = {
    name: 'Логика B (Старый дашборд)',
    trigger: (data) => {
        return data.some(row => 
            row.operation.includes('пикинг зх мезонин rmixed')
        );
    },
    charts: [
        {
            id: 'receiving',
            title: 'Приёмка (шт)',
            operations: ['приемка'],
            metric: 'qtyPieces',
            norm: 120,              // ⬅️ ИЗМЕНИТЕ ЗДЕСЬ
            unit: 'шт'
        },
        {
            id: 'placement',
            title: 'Размещение (короба)',
            operations: ['ручные перемещения', 'привязка к местам'],
            metric: 'qtyPackages',
            norm: 250,              // ⬅️ ИЗМЕНИТЕ ЗДЕСЬ
            unit: 'коробов'
        },
        {
            id: 'picking',
            title: 'Сборка (задачи)',
            operations: ['пикинг зх мезонин rmixed'],
            metric: 'operationCount',
            norm: 300,              // ⬅️ ИЗМЕНИТЕ ЗДЕСЬ
            unit: 'задач'
        }
    ]
};
```

### Логика A (Новый дашборд)

Найдите секцию `LOGIC_A` и измените значения `norm`:

```javascript
const LOGIC_A = {
    name: 'Логика A (Новый дашборд)',
    trigger: (data) => true,
    charts: [
        {
            id: 'mop_picking',
            title: 'MOP Сборка',
            operations: ['пикинг mop зона хранения мезонин'],
            metric: 'operationCount',
            norm: 300,              // ⬅️ ИЗМЕНИТЕ ЗДЕСЬ
            unit: 'задач'
        },
        {
            id: 'mop_receiving',
            title: 'MOP Приёмка',
            operations: ['приемка'],
            metric: 'qtyPieces',
            norm: 120,              // ⬅️ ИЗМЕНИТЕ ЗДЕСЬ
            unit: 'шт'
        },
        {
            id: 'mop_placement',
            title: 'MOP Размещение',
            operations: ['перемещения', 'привязка к местам', 'ручные перемещения'],
            metric: 'qtyPackages',
            norm: 250,              // ⬅️ ИЗМЕНИТЕ ЗДЕСЬ
            unit: 'коробов'
        },
        {
            id: 'mop_control',
            title: 'MOP Контроль',
            operations: ['контроль'],
            metric: 'operationCount',
            norm: 200,              // ⬅️ ИЗМЕНИТЕ ЗДЕСЬ
            unit: 'операций'
        }
    ]
};
```

---

## Изменение операций

### Пример 1: Добавление новой операции к существующему графику

Если нужно добавить операцию "новая операция" к графику "MOP Размещение":

```javascript
{
    id: 'mop_placement',
    title: 'MOP Размещение',
    operations: [
        'перемещения', 
        'привязка к местам', 
        'ручные перемещения',
        'новая операция'        // ⬅️ ДОБАВЬТЕ ЗДЕСЬ
    ],
    metric: 'qtyPackages',
    norm: 250,
    unit: 'коробов'
}
```

### Пример 2: Изменение названия операции

Если в Excel изменилось название операции с "приемка" на "приём товара":

```javascript
{
    id: 'receiving',
    title: 'Приёмка (шт)',
    operations: ['приём товара'],    // ⬅️ ИЗМЕНИТЕ ЗДЕСЬ
    metric: 'qtyPieces',
    norm: 120,
    unit: 'шт'
}
```

**Важно:** Названия операций автоматически приводятся к нижнему регистру, поэтому регистр не важен.

---

## Изменение цветов

### Цвета индикации нормы

```javascript
const CONFIG = {
    // ...
    COLORS: {
        LOW: '#ff3b3b',    // ⬅️ Красный (0-70%)
        MID: '#ffcc00',    // ⬅️ Жёлтый (70-100%)
        HIGH: '#00b050'    // ⬅️ Зелёный (100%+)
    }
};
```

### Цвета ТОП-3

Найдите в CSS секцию:

```css
.top-performers li:nth-child(1) {
    background: #ffd700;         /* ⬅️ Золото (1 место) */
    border-left-color: #ffa500;
    font-weight: bold;
    color: #000;
}
.top-performers li:nth-child(2) {
    background: #c0c0c0;         /* ⬅️ Серебро (2 место) */
    border-left-color: #999;
    color: #000;
}
.top-performers li:nth-child(3) {
    background: #cd7f32;         /* ⬅️ Бронза (3 место) */
    border-left-color: #8b5a00;
    color: #fff;
}
```

### Цвета темы

```css
body {
    background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);  /* ⬅️ Фон */
    color: #eee;  /* ⬅️ Текст */
}

.container {
    background: #0f1419;  /* ⬅️ Фон контейнера */
}

.chart-container {
    background: #1a1f2e;  /* ⬅️ Фон графиков */
    border: 1px solid #2a2f3e;  /* ⬅️ Бордюр */
}
```

---

## Изменение интервала обновления

### Автоматическое обновление

```javascript
const CONFIG = {
    REFRESH_INTERVAL_MS: 60000,    // ⬅️ 60000 = 60 секунд
    // ...
};
```

**Примеры:**
- 30 секунд: `30000`
- 2 минуты: `120000`
- 5 минут: `300000`

---

## Добавление новых графиков

### Шаг 1: Определите параметры графика

```javascript
{
    id: 'my_new_chart',                    // Уникальный ID
    title: 'Моя новая метрика',            // Название графика
    operations: ['операция 1', 'операция 2'], // Операции из Excel
    metric: 'qtyPieces',                   // Метрика для подсчёта
    norm: 150,                             // Норматив
    unit: 'шт'                             // Единица измерения
}
```

### Шаг 2: Добавьте график к нужной логике

#### Для Логики A:

```javascript
const LOGIC_A = {
    name: 'Логика A (Новый дашборд)',
    trigger: (data) => true,
    charts: [
        // ... существующие графики ...
        {
            id: 'my_new_chart',
            title: 'Моя новая метрика',
            operations: ['операция 1', 'операция 2'],
            metric: 'qtyPieces',
            norm: 150,
            unit: 'шт'
        }
    ]
};
```

#### Для Логики B:

```javascript
const LOGIC_B = {
    name: 'Логика B (Старый дашборд)',
    trigger: (data) => {
        return data.some(row => 
            row.operation.includes('пикинг зх мезонин rmixed')
        );
    },
    charts: [
        // ... существующие графики ...
        {
            id: 'my_new_chart',
            title: 'Моя новая метрика',
            operations: ['операция 1', 'операция 2'],
            metric: 'qtyPieces',
            norm: 150,
            unit: 'шт'
        }
    ]
};
```

### Шаг 3: Доступные метрики

```javascript
// Метрики из Excel файла:
'operationCount'    // Количество операций
'qtyPieces'         // Кол-во, шт
'qtyPackages'       // Кол-во, уп
'qtyLines'          // Кол-во, строк
'weight'            // Вес
'volume'            // Объем
'avgTime'           // Среднее время выполнения операции
'totalTime'         // Общее время выполнения всех операций
'workDuration'      // Длительность работы (в секундах)
```

---

## Изменение порта сервера

Откройте файл `server.js` и найдите:

```javascript
const PORT = 3000;    // ⬅️ ИЗМЕНИТЕ ЗДЕСЬ
const HOST = '0.0.0.0';
```

**Примеры:**
- Порт 8080: `const PORT = 8080;`
- Порт 5000: `const PORT = 5000;`

После изменения порта открывайте дашборд по новому адресу:
- `http://localhost:8080` (если установили порт 8080)
- `http://localhost:5000` (если установили порт 5000)

---

## Изменение условия переключения логик

Если нужно изменить условие, при котором активируется Логика B:

```javascript
const LOGIC_B = {
    name: 'Логика B (Старый дашборд)',
    trigger: (data) => {
        // ⬅️ ИЗМЕНИТЕ УСЛОВИЕ ЗДЕСЬ
        return data.some(row => 
            row.operation.includes('пикинг зх мезонин rmixed')
        );
    },
    charts: [
        // ...
    ]
};
```

**Примеры условий:**

### Проверка нескольких операций (ИЛИ)

```javascript
trigger: (data) => {
    return data.some(row => 
        row.operation.includes('операция 1') ||
        row.operation.includes('операция 2')
    );
}
```

### Проверка нескольких операций (И)

```javascript
trigger: (data) => {
    const hasOp1 = data.some(row => row.operation.includes('операция 1'));
    const hasOp2 = data.some(row => row.operation.includes('операция 2'));
    return hasOp1 && hasOp2;
}
```

### Проверка по дате

```javascript
trigger: (data) => {
    const today = dayjs();
    return data.some(row => 
        row.date && row.date.isSame(today, 'day')
    );
}
```

### Проверка по количеству записей

```javascript
trigger: (data) => {
    return data.length > 100;  // Логика B если больше 100 записей
}
```

---

## Изменение количества дней для фильтрации

По умолчанию дашборд показывает данные за последние 30 дней:

```javascript
const CONFIG = {
    DATE_RANGE_DAYS: 30,    // ⬅️ ИЗМЕНИТЕ ЗДЕСЬ
    // ...
};
```

**Примеры:**
- 7 дней: `DATE_RANGE_DAYS: 7`
- 14 дней: `DATE_RANGE_DAYS: 14`
- 60 дней: `DATE_RANGE_DAYS: 60`
- Все данные: `DATE_RANGE_DAYS: 36500` (100 лет)

---

## Изменение количества сотрудников в ТОП-листе

По умолчанию показывается ТОП-3:

```javascript
const CONFIG = {
    TOP_N: 3,    // ⬅️ ИЗМЕНИТЕ ЗДЕСЬ
    // ...
};
```

**Примеры:**
- ТОП-5: `TOP_N: 5`
- ТОП-10: `TOP_N: 10`
- Все сотрудники: `TOP_N: 999`

**Важно:** При изменении TOP_N также нужно настроить CSS для дополнительных мест:

```css
.top-performers li:nth-child(4) {
    background: #e6e6fa;
    border-left-color: #9370db;
    color: #000;
}
.top-performers li:nth-child(5) {
    background: #ffe4e1;
    border-left-color: #ff69b4;
    color: #000;
}
```

---

## Примеры полной конфигурации

### Пример 1: Быстрое обновление для тестирования

```javascript
const CONFIG = {
    EXCEL_FILE_ENDPOINT: '/excel',
    REFRESH_INTERVAL_MS: 5000,       // 5 секунд
    DATE_RANGE_DAYS: 7,              // Только последняя неделя
    TOP_N: 5,                        // ТОП-5
    COLORS: {
        LOW: '#ff3b3b',
        MID: '#ffcc00',
        HIGH: '#00b050'
    }
};
```

### Пример 2: Длительный период с редким обновлением

```javascript
const CONFIG = {
    EXCEL_FILE_ENDPOINT: '/excel',
    REFRESH_INTERVAL_MS: 300000,     // 5 минут
    DATE_RANGE_DAYS: 90,             // 3 месяца
    TOP_N: 10,                       // ТОП-10
    COLORS: {
        LOW: '#ff3b3b',
        MID: '#ffcc00',
        HIGH: '#00b050'
    }
};
```

---

## Сохранение изменений

После внесения изменений:

1. **Сохраните файл** `dashboard.html` или `server.js`
2. **Остановите сервер** (Ctrl+C в консоли)
3. **Запустите заново** `start.bat`
4. **Обновите страницу** в браузере (F5 или Ctrl+F5)

---

## Резервное копирование

Перед внесением изменений рекомендуется создать резервную копию:

```
dashboard.html → dashboard.html.backup
server.js → server.js.backup
```

В случае ошибки вы сможете вернуться к исходной версии.

---

**Нужна помощь?** Проверьте консоль браузера (F12) на наличие ошибок JavaScript.
