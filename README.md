# Производственный Дашборд

Полнофункциональный дашборд для отображения производственных операций с двумя логиками построения графиков, автоматическим обновлением и поддержкой работы без прав администратора.

## 📋 Возможности

- ✅ **Две логики построения графиков** (автоматический выбор)
- ✅ **Динамическое отображение** только тех графиков, для которых есть данные
- ✅ **ТОП-3 сотрудников** по каждой метрике
- ✅ **Цветовая индикация** достижения нормативов (красный/жёлтый/зелёный)
- ✅ **Горизонтальные линии норм** на графиках
- ✅ **Автоматическое обновление** каждые 60 секунд
- ✅ **Автоматический поиск** первого Excel файла в папке
- ✅ **Тёмная тема** для отображения на телевизоре
- ✅ **Работа без прав администратора** (портативный Node.js)

## 🎯 Логики построения графиков

### Логика B (Старый дашборд)

**Триггер:** Срабатывает только если в данных есть операция `"пикинг ЗХ МЕЗОНИН RMIXED"`

**Графики:**
- **Приёмка** → операция "приемка" (метрика: штуки)
- **Размещение** → операции "ручные перемещения" + "привязка к местам" (метрика: коробы)
- **Сборка** → операция "пикинг ЗХ МЕЗОНИН RMIXED" (метрика: количество операций)

### Логика A (Новый дашборд)

**Триггер:** Срабатывает во всех остальных случаях

**Графики:**
- **MOP Сборка** → операция "пикинг MOP Зона Хранения Мезонин"
- **MOP Приёмка** → операция "приемка"
- **MOP Размещение** → операции "перемещения", "привязка к местам", "ручные перемещения"
- **MOP Контроль** → операция "контроль"

**Важно:** Отображаются только те графики, для которых есть данные!

## 📦 Установка

### Шаг 1: Скачивание портативного Node.js

1. Перейдите на сайт: https://nodejs.org/dist/latest/
2. Скачайте версию для Windows (например, `node-v20.x.x-win-x64.zip`)
3. Распакуйте архив в папку `node` внутри папки с дашбордом

### Шаг 2: Структура проекта

После установки структура должна выглядеть так:

```
Dashboard/
│
├── node/                    ← Портативный Node.js
│   ├── node.exe
│   ├── npm
│   └── ...
│
├── dashboard.html           ← Главный файл дашборда
├── server.js                ← Node.js сервер
├── start.bat                ← Файл запуска
│
└── ваш_файл.xlsx           ← Excel файл с данными (любое имя)
```

### Шаг 3: Excel файл

Поместите Excel файл с данными в папку `Dashboard`. Файл может иметь любое имя, главное чтобы расширение было `.xlsx`.

**Требуемые колонки:**
- Производственный ресурс
- Дата операций
- Название операции
- Количество операций
- Среднее время выполнения операции
- Общее время выполнения всех операций
- Время начала работы
- Время окончания работы
- Длительность работы
- Кол-во, шт
- Кол-во, уп
- Кол-во, строк
- Вес
- Объем
- qwe
- qwe2

## 🚀 Запуск

1. **Двойной клик** на файл `start.bat`
2. Откроется консоль с информацией о запуске
3. Откройте браузер и перейдите по адресу: **http://localhost:3000**

## 🔄 Обновление данных

### Автоматическое обновление

- Дашборд автоматически обновляется **каждые 60 секунд**
- При обновлении Excel файла дашборд автоматически загрузит новые данные
- Не нужно обновлять страницу вручную!

### Замена Excel файла

1. Просто замените старый Excel файл новым в папке `Dashboard`
2. Имя файла может быть любым
3. Дашборд автоматически найдёт и загрузит новый файл при следующем обновлении

## 📊 Интерфейс дашборда

### Заголовок

- Время последнего обновления
- Индикатор текущей логики (A или B)

### Графики

Для каждой метрики отображается:

1. **Столбчатая диаграмма** с данными по каждому сотруднику
2. **Цветовая индикация:**
   - 🔴 Красный: 0-70% от нормы
   - 🟡 Жёлтый: 70-100% от нормы
   - 🟢 Зелёный: 100%+ от нормы
3. **Горизонтальная линия нормы** (красная пунктирная)
4. **ТОП-3 сотрудника:**
   - 🥇 1 место — золотой фон
   - 🥈 2 место — серебряный фон
   - 🥉 3 место — бронзовый фон

### Блок нормативов

Внизу страницы отображаются нормативы для всех активных метрик.

## ⚙️ Настройка

### Изменение нормативов

Откройте файл `dashboard.html` и найдите секцию с определением логик:

```javascript
const LOGIC_B = {
    charts: [
        {
            id: 'receiving',
            title: 'Приёмка (шт)',
            operations: ['приемка'],
            metric: 'qtyPieces',
            norm: 120,  // ← Измените норму здесь
            unit: 'шт'
        },
        // ...
    ]
};
```

### Изменение интервала обновления

В файле `dashboard.html` найдите:

```javascript
const CONFIG = {
    REFRESH_INTERVAL_MS: 60000,  // ← 60000 = 60 секунд
    // ...
};
```

### Изменение порта сервера

В файле `server.js` найдите:

```javascript
const PORT = 3000;  // ← Измените порт здесь
```

## 🔧 Устранение неполадок

### Проблема: "Портативный Node.js не найден"

**Решение:**
1. Убедитесь, что папка `node` существует
2. Внутри папки `node` должен быть файл `node.exe`
3. Скачайте портативную версию Node.js и распакуйте её в папку `node`

### Проблема: "Excel файл не найден"

**Решение:**
1. Убедитесь, что в папке `Dashboard` есть файл с расширением `.xlsx`
2. Проверьте, что файл не открыт в Excel (закройте его)

### Проблема: "Порт 3000 уже используется"

**Решение:**
1. Закройте другие программы, использующие порт 3000
2. Или измените порт в файле `server.js`

### Проблема: "Не отображаются графики"

**Решение:**
1. Проверьте, что в Excel файле есть данные за последние 30 дней
2. Убедитесь, что колонка "Название операции" содержит правильные названия операций
3. Откройте консоль браузера (F12) и проверьте наличие ошибок

### Проблема: "Дашборд не обновляется автоматически"

**Решение:**
1. Проверьте, что сервер запущен и работает
2. Откройте консоль браузера (F12) и проверьте, есть ли ошибки сети
3. Попробуйте обновить страницу вручную (F5)

## 📱 Использование на телевизоре

1. Подключите компьютер с запущенным дашбордом к локальной сети
2. Узнайте IP-адрес компьютера (команда `ipconfig` в консоли)
3. На Smart TV откройте браузер и введите адрес: `http://[IP-адрес]:3000`
4. Переключите браузер в полноэкранный режим

**Пример:** Если IP-адрес компьютера `192.168.1.100`, то адрес будет:
```
http://192.168.1.100:3000
```

## 🎨 Особенности дизайна

- **Тёмная тема** для комфортного просмотра на телевизоре
- **Адаптивная вёрстка** (автоматически подстраивается под размер экрана)
- **Крупный текст** для хорошей читаемости на расстоянии
- **Контрастные цвета** для чёткого различения данных

## 📝 Технические детали

### Технологии

- **Frontend:** HTML, CSS, JavaScript
- **Backend:** Node.js (встроенный модуль `http`)
- **Библиотеки:**
  - Chart.js 4.4.0 — построение графиков
  - chartjs-plugin-annotation 3.0.1 — линии норм
  - XLSX 0.18.5 — чтение Excel файлов
  - Day.js 1.11.10 — работа с датами
  - Lodash 4.17.21 — утилиты для работы с данными

### Обработка данных (ETL)

1. **Extract** — загрузка Excel файла
2. **Transform** — нормализация дат, парсинг чисел, фильтрация по дате
3. **Load** — группировка по сотрудникам, вычисление метрик

### Безопасность

- Нет зависимостей от npm (все библиотеки загружаются с CDN)
- Работает полностью локально (нет отправки данных в интернет)
- Не требует прав администратора

## 🆘 Поддержка

Если у вас возникли проблемы:

1. Проверьте раздел "Устранение неполадок"
2. Откройте консоль браузера (F12) и проверьте наличие ошибок
3. Проверьте консоль сервера на наличие сообщений об ошибках

## 📄 Лицензия

Проект распространяется свободно для внутреннего использования на складах.

---

**Версия:** 1.0  
**Дата:** 2024  
**Разработано для:** Складские операции
